You are a Senior Frontend Architect.

Analyze the existing Freemarker (FTL) + JavaScript implementation and fix all SSE lifecycle issues WITHOUT changing any backend code.

Important Constraints:

* Backend SSE API already exists.
* Backend code cannot be modified.
* Solution must be implemented entirely in frontend.
* Existing SSE endpoint contract must remain unchanged.

Current Problem:
SSE connection is lost when:

* User refreshes the page
* User navigates to another screen
* User uses browser back/forward
* User opens multiple tabs
* User temporarily loses network
* FTL pages reload

Expected Behaviour:

1. Download tracking must continue seamlessly.
2. User should never feel the download was lost.
3. Active download jobs must survive refresh.
4. Active download jobs must survive page navigation.
5. Active download jobs must restore automatically after browser reopen.
6. Multiple concurrent downloads must be supported.
7. Duplicate EventSource connections must be prevented.
8. Duplicate event listeners must be prevented.
9. Memory leaks must be eliminated.
10. Automatic reconnection must happen on failures.
11. UI state must recover automatically.

Implementation Requirements:

Review:

* All EventSource creation points.
* All page lifecycle hooks.
* All navigation handling.
* All localStorage/sessionStorage usage.
* All download tracking logic.

Implement:

1. Centralized SSE Manager singleton.
2. Connection reuse strategy.
3. Active Job Registry.
4. localStorage persistence.
5. Browser reopen recovery.
6. Reconnection strategy with exponential backoff.
7. Network offline/online recovery.
8. Visibility change handling.
9. Multi-tab coordination.
10. Cleanup of stale subscriptions.

Architecture:

* Store active download job IDs in localStorage.
* On page load automatically restore active jobs.
* Re-subscribe to required SSE streams.
* If SSE reconnect fails repeatedly, use status API fallback if available.
* Maintain download state independently from page lifecycle.

Deliverables:

1. Root cause analysis.
2. Exact issues found.
3. Complete code modifications.
4. Refactored JavaScript implementation.
5. Event flow explanation.
6. Multi-tab handling strategy.
7. Refresh recovery strategy.
8. Browser reopen recovery strategy.

Do not provide theoretical suggestions.
Provide complete production-ready code compatible with Freemarker (FTL) pages.
